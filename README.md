# X-PROJECT

`X-project` -- это кодовое название нашего проекта решения цифровой платформы проекта «Креатур», который предствляет из себя набор из 2 модулей `/server` и `/client`, написанные на ЯП `typescript` на NodeJS в связке с СУБД MongoDB, с используемым стеком: `mongodb` и `@typegoose/typegoose` для СУБД MongoDB;  `@apollo/server`, `@apollo/client`, `type-graphql` и `class-validator` для языка запросов GraphQL; `@apollo/server/express4` для REST API (для особых случаев); `@accounts/*`, `express-jwt` и `argon2` для авторизации и аунтефикации, использующие `jsonwebtoken`; `@apollo/client`, `react`, `redux` для frontend-стороны; `dotenv` для конфигурации из `.env` файла.

### <b>Ремарка:</b>
<b>Просим прощения!!!</b>
> Наш проект слишком сырой. В нем много недоработанных частей. Мы слишком увлеклись разработкой первого модуля, поэтому слишком поздно начали разрабатывать второй модуль. Разрываясь между разработкой обоих модулей, мы не смогли сделать ни один из модулей качественно.

# Установка и запуск

Скачайте проект с [github по сслыке](https://github.com/Be3y4uu-K0T/X-project), либо командой:
```
git clone https://github.com/Be3y4uu-K0T/X-project.git
```

Для запуска проекта потребуется использовать инструменты: СУБД MongoDB, среду выполнения кода NodeJS, а также менежер пакетов Yarn.

## Установка СУБД MongoDB:

Так как облачный сервис MongoDB Atlas отключен для РФ, мы будем использовать локальную версию MongoDB.

### Для Windows:

> [Отличная статья для установки](https://metanit.com/nosql/mongodb/1.2.php).

Кратко из статьи:

Установить можно по [ссылке из офицального сайта](https://www.mongodb.com/try/download/community), либо для Windows по прямой [ссылке](https://fastdl.mongodb.org/windows/mongodb-windows-x86_64-6.0.0.zip), либо с [Яндекс диска (zip-архив)](https://disk.yandex.ru/d/eXcLoxjYg11z-w).

В ОС Windows по умолчанию MongoDB хранит базы данных по пути `C:\data\db`, вам надо создать соответствующий каталог, либо же использовать при запуске MongoDB специальный флаг `--dbpath` для `mongod` и указать собственный путь для хранения баз данных.

Далее нужно запустить `mongod` сервис, который находится в каталоге `bin` в папке сервера (пример: `C:\mongodb\bin\mongod`). Для этого запустим терминал/командную строку и там введем соответствующие команды. Пример:
```
C:\mongodb\bin\mongod.exe --dbpath C:\data\db
```

### Для MacOS:

Можно установить с помощью пакетного менеджера homebrew:
```
brew install mongodb-community
```

И последующий запуск:
```
brew services start mongodb-community
```

#### Ссылка на подключение:

Обычно адрес для использования MongoDB: `mongodb://localhost:27017/<имя-базы-данных>`.

Мы будем использовать одноименную нашему проекту базу данных `x-project`, поэтому в `.env` файле указываем для `MONGO_DB_URI` значение `mongodb://127.0.0.1:27017/x-project`.

## Установка NodeJS.

> [Отличная статья для установки NodeJS](https://metanit.com/web/nodejs/1.1.php).

Можно установить с [офицального сайта](https://nodejs.org/en/).

Либо через пакетный менеджер homebrew:
```
brew install node
```

Мы будем использовать версию NodeJS 19.4.0:
```
$ node --version
v19.4.0
```

Дополнительно должен установиться стандартный пакетный менеджер `npm`, через него мы установим глобально специальный пакетный менеджер `yarn`:
```
npm install --global yarn
```

Пример (после установки yarn):
```
$ yarn --version
1.22.19
```

## Установка зависимостей для проекта

Воспользуемся специальной командой для `yarn`, который должен установить всё пакеты из файлов `yarn.lock`, которая описана в `package.json`:
```
yarn run install-all
```

## Конфигурация/Настройка проекта

В файле `.env` хранятся несколько значений среды:

- NODE_ENV -- состояние проекта `development` или `production`, по умолчанию `development`;
- PORT -- порт на котором запуститься проект (сервер), по умолчанию `4000`;
- MONGO_DB_URI -- адрес подключения к базе данных, по умолчанию `mongodb://localhost:27017/x-project`;
- JWT_PRIVATE_KEY -- приватный ключ RSA-256 закодированный в Base64 для JWT, по умолчанию заранее сгенерированный тестовый ключ на онлайн сервисе.
- JWT_PUBLIC_KEY -- публичный ключ RSA-256 закодированный в Base64 для JWT, по умолчанию заранее сгенерированный тестовый ключ на онлайн сервисе.
- JWT_ACCESS_TOKEN_LIFETIME -- время жизни access-токена, по умолчанию `90m` (90 минут).
- JWT_REFRESH_TOKEN_LIFETIME -- время жизни refresh-токена 7d (7 дней).

### Ремарка:
Пока временно в проекте не настроено другие варианты шифрования, кроме как RSA-256 (RS256).
Планируется позже переписать конфигурацию и создать отдельный файл `config.mts`, который

## Запуск проекта

> В файле `package.json` описаны два скрипта -- `build` и `start`. Первый скрипт отвечает за компиляцию всего проекта из `typescript` в `javascript` и связки всего проекта. Второй скрипт вначале выполняет первый, а пото запускает проект (сервер).

Запускаем проект (пример):
```
yarn start

...

🚀 Server listening at: http://localhost:4000/ 🚀
```
Дальше переходим по указанному url (http://localhost:4000/), на котором запустился наш сайт, Et Voilà! У нас что-то есть 😁 🎉

# Использование проекта

## Backend-часть

В режиме `NODE_ENV=development` по стандратному адресу `http://localhost:4000/api` можно зайти в apollo sanbox, где можно протестировать различные GraphQL запросы:
- `Query` - это GET запросы, которые не изменяют данные на сервере.
- `Mutation` - это POST/PUT/DELETE (и так далее) запросы, которые изменяют данные на сервере.

> Далее в примерах наш сервер запущен по адресу `http://localhost:4000/`

Для запросов мы использовали подход GraphQL вместо REST API, кроме как в случае загрузки вложений на сайте. На сайте (только через запросы пока что) можно через запрос `addAttachment` добавить вложение и использовать его в мероприятиях, комментариях, отзывах и так далее. В вложение передается адрес (uri), где он хранится в интернете. Если пользоатель хочет загрузить новое изобрадение, то web-клиент вначале загружает его по адресу `http://localhost:4000/cdn`, а сервер возращает uri, который можно потом переиспользовать в новых вложениях.

Зарегистрироваться на сайте можно через запросы:
- `signup_as_client`
- `signup_as_developer`
- TODO: другие роли

Нужно минимум для клиента передать адрес электронной почты и придуманный пароль, сервер вернет объект зарегистрированного клиента.

Далее нужно авторизироваться через запрос `signin`. Нужно передать адрес электронной почты и пароль от аккаунта, сервер вернет объект сеанса, который хранит индефикатор сессии и токены (access и refresh).

Далее нужно клиенту передавать access-токен для доступа к определенным ресурсам, а refresh-токен, чтобы получить новые токены.

## Frontend-часть

### TODO: <b>Просим прощения!</b>

Мы хотели использовать React для интерфейса, Redux для хранения состояний, и Apollo/Client для запросов GraphQL, а также JsonWebToken для аутентификации.

JsonWebToken мы генерируем для доступа к ресурсам (Пример):
```
eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..V8lZRSjoPOdn7S6bUfXoC53v7iNgzYPS_1xzZ8OaCw77cx-fuQjBeoWjLQz2v-xWQdUleslw_vigtJwT4eAqVU0uRQPTsakNBovpg4r2plRb3-6fm-7Z5MSLG3FluKBy8ajrbG2d8VrD39aJV8xhLUuXgR6wSF12HVl5_T9FUkE
```
Декодированный:
```
header: {
  "alg": "RS256",
  "typ": "JWT"
}
payload: {
  "role": "ADMIN",
  "data": {
    "token": "a223edd4cc3b6c2ab44ac03ff89cd43ce18937f58c9cbc3544824a08e1161d802c4a1335647d510f24e8e3",
    "isImpersonated": false,
    "userId": "63d2a1989fd1617ffd3ead51"
  },
  "iat": 1674796239,
  "exp": 1674801639
}
verify signature: RSASHA256(
  base64UrlEncode(header) + "." +
  base64UrlEncode(payload),
  PRIVATE_KEY
)
```
- header -- хранит информацию о самом jwt-токене
    - alg -- тип шифрования для подписи
    - typ -- тип токена для payload (полезной нагрузки)
- payload -- полезная нагрузка
    - role -- роль, под которой зарегистрировался пользователь
    - data -- данные от accounts-js
        - token -- токен сессии
        - isImpersonated -- флаг, что пользователь выдает себя за другог. Это нужно в некторых ситуация, чтобы посмотртеть от лица другого пользователя.
        - userId -- индефикатор пользователя
    - iat -- Когда выпущен токен: Fri Jan 27 2023 10:10:39 GMT+0500 (Yekaterinburg Standard Time)
    - exp -- Время "смерти" токена: Fri Jan 27 2023 11:40:39 GMT+0500 (Yekaterinburg Standard Time)
- verify signature -- подпись подтверждающая, что токен выдан нашим сервером или сервисом

Далее токен хранится должен был в cookie с флагом httpOnly и передаваться в запросах в заголовке Authorization со значеним `Bearer <token>`.

## Послесловие

Мы многое не успели сделать. Даже то, что обязательно требовалось:
- видео-презентацию нашего продукта:
    - Мы решили не делать, потому что не успевали, а продукта по факту нет всё ещё.
- документа (ppt или doc), доказывающий, что мы правильно поняли техническое задание:
    - Мы хотели сделать две схемы нашего проекта -- схему бизнес-процессов и схему физической структуры базы данных; В определенной степени, хоть и не в том форате, schema.gql описывает, что хранит сервер, единственное ещё существет коллекции `auth_users` и `auth_sessions`, которые хранят конфедициальные данные -- токены пользователей и данные сесии соответсвтенно.
- множество требований к модулю 2, в нем почти ничего нет и не возможно с ним взаимодействовать:
    - здесь <u>полностью наша вина</u>
- сервер (модуль 1) не имеет возможности полного взаимодействия и доступа по ролям:
    - здесь <u>полностью наша вина</u>

Это проект имеет право на жизнь, возможно, если было бы ещё время, после того, как мы осознали сложность данного проекта, то мы сделали более все качественно, но здесь мы сами виноваты, что откладывали и медленно начинали делать до сроков сдачи. Благодарим организаторов за дачу дополнительного времени (сдвиг сдачи до 30 числа), это дало жизнь проекту, что мы хотя бы успели перейти ко второму модулю! Спасибо Вам!!!
